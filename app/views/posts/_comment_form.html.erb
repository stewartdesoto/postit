<div class="row">
  <%= form_for [@post, @comment] do |f| %>
    <%= render 'shared/error_messages', obj: @comment %>
    <div class='control-group'
      <%= f.label :body, "Leave a comment" %>
      <%= f.text_area :body, rows:5, class:'col-md-12' %>
    </div>
    <%= f.submit "Add comment", class: "btn btn-primary" %>
  <% end %>
</div>